## IMPORTANT — CLI Mode Output Override
 
You are running in CLI mode. You cannot write files.
Ignore any instruction above that says to write to `docs/diagrams/`.
Output ONLY the raw Mermaid diagram text — no explanations, no descriptions, no markdown fencing (no ```mermaid), no bullet points, no commentary.
The very first line of your response must be a `%%` metadata comment.