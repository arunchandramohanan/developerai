Important: Tag all AI-generated code using the file's native comment syntax and the following rules:
 
- If you generate or rewrite an **entire file**, add at the very top of the file:
  `// @ai-generated: DevAI | date: {{date}}`
- If you generate or modify a **function/method**, add directly above the function signature:
  `// @ai-generated: DevAI | date: {{date}}`
- If you generate or modify a **single line or expression**, add an inline comment at the end of that line:
  `/* @ai-generated: DevAI | date: {{date}} */`
 
Always use the file's native comment syntax (e.g., `#` for Python/YAML, `//` for Java/JS/TS, `--` for SQL).
